name: Test Arch Setup Script (Dry Run)

on:
  push:
    paths:
      - "install.sh"
  pull_request:
    paths:
      - "install.sh"

jobs:
  test-arch:
    name: 🧪 Dry Run on Arch Linux
    runs-on: ubuntu-latest

    container:
      image: archlinux:base

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v3

      - name: 🛠 Install base tools
        run: |
          pacman -Sy --noconfirm sudo git base-devel fish
          useradd -m sb74
          echo "sb74 ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers

      - name: 🚀 Run setup script in DRY_RUN mode
        run: |
          chmod +x ./install.sh
          DRY_RUN=true sudo -E ./install.sh <<< "y"
